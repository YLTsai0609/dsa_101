# traversing 

``` Python
Algorithm display(e)
    p = head
    i = 0
    while i < length()
        print(p.element)
        p = p.next
        i +=1
```

Time complexity : $O(N)$

Space compleixty : $O(1)$
